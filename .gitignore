# secrets
.env
.env.*
!.env.example

# local-only URLs / sync dumps (never commit)
google_webhook_url
grook-local-sync/
*.tgz

# runtime data (last @ timestamp, etc.)
data/

# deps & build
node_modules/
dist/

# logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db
